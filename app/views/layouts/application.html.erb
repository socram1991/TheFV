<!DOCTYPE html>
<html>
  <head>
    <title>Thereviewproject</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <nav class="navbar navbar-default">

    <div class="container">

      <div class="navbar-header ">
        <%= link_to "Putt-Putt Review", root_path, class: "navbar-brand" %>
      </div>

      <ul class="nav navbar-nav navbar-default" >
        <li><%= link_to "Sign Up", new_user_registration_path %></li>
        <% if user_signed_in? %>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Log In", new_user_session_path %></li>
        <% end %>
      </ul>



      <ol class="nav navbar-nav navbar-right navbar-default ">

        <li class="nav-item dropdown">



          <ul class="dropdown" role="menu" aria-labelledby="someID">
            <% Category.all.each do |category| %>
                <li class="dropdown <%= 'active' if params[:category] == category.name %>">
                  <%= link_to category.name, attractions_path(category: category.name), class: "link" %>
                </li>
            <% end %>
          </ul>

        </li  >

      <% if user_signed_in? %>
        <li><%= link_to "Add Item", new_attraction_path %></li>
      <% end %>
      </ol>


    </div>

  </nav>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

<div class="container">

  <%= yield %>

</div>
  </body>
</html>
